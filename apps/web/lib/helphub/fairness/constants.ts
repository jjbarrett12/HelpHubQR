/** Closing shifts in lookback before logging `undesirable_shift_repeated` (ledger + hints aligned). */
export const CLOSE_SHIFT_REPEAT_LEDGER_THRESHOLD = 6;

/** Hint when count is high but below ledger threshold (advisory only). */
export const CLOSE_SHIFT_REPEAT_HINT_THRESHOLD = 5;
